import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Phase 1 scaffold: no custom config needed yet.
  // Later plans add image remote patterns (Supabase Storage), headers, etc.
};

export default nextConfig;

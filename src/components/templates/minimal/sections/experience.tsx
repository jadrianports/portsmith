/**
 * Experience section (D-05 section 5). STUB — frozen against the SHARED
 * `SectionProps` contract; a Wave-4/5 plan replaces ONLY this body.
 *
 * When implemented: cast `section?.content` to `ExperienceContent`, render mono
 * dates (`tnum`), map `'present'` → "Present". Read `--token`s, return `null` when
 * no items.
 */
import type { SectionProps } from './types';

export function Experience({ section }: SectionProps) {
  void section;
  return null;
}

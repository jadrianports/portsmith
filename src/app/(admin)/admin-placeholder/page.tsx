/**
 * PLACEHOLDER — (admin) route group marker.
 *
 * Exists only so the (admin) group has a renderable route for the build check.
 * Minimal admin tooling lands in a later phase.
 */
export default function AdminPlaceholder() {
  return <p>admin placeholder — later phase</p>;
}

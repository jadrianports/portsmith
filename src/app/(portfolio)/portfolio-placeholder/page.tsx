/**
 * PLACEHOLDER — Phase 3 (portfolio) route group marker.
 *
 * Exists only so the (portfolio) group has a renderable route for the build
 * check. The real public `/[username]` page (ISR) replaces this in Phase 3.
 */
export default function PortfolioPlaceholder() {
  return <p>portfolio placeholder — Phase 3</p>;
}

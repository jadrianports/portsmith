/**
 * PLACEHOLDER — Phase 4 (dashboard) route group marker.
 *
 * Exists only so the (dashboard) group has a renderable route for the build
 * check. The real CMS dashboard replaces this in Phase 4.
 */
export default function DashboardPlaceholder() {
  return <p>dashboard placeholder — Phase 4</p>;
}

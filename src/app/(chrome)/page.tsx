/**
 * Phase-1 placeholder root route.
 *
 * The real marketing / landing experience is built in a later phase. This exists
 * only so the scaffold has a renderable index route for the build check.
 */
export default function Home() {
  return (
    <main>
      <h1>Portsmith</h1>
      <p>Foundation scaffold — UI lands in later phases.</p>
    </main>
  );
}

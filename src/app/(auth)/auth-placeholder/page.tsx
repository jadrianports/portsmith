/**
 * PLACEHOLDER — Phase 2 (auth) route group marker.
 *
 * Exists only so the (auth) group has a renderable route for the build check.
 * Real auth pages (sign in / sign up / reset) replace this in Phase 2.
 */
export default function AuthPlaceholder() {
  return <p>auth placeholder — Phase 2</p>;
}
